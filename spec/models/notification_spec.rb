require 'rails_helper'

RSpec.describe Notification, type: :model do
  # describe "can create a notification" do
  #
  #   before do
  #     @user = create_user
  #     @project = create_project
  #     @notif = Notification.create(user: @user, payload: @project, notification_type_id: 1)
  #   end
  #   it "should create a notification " do
  #     expect(@notif.notification_type).to eq("project_released")
  #     expect(@user.notifications.last).to eq(@notif)
  #   end
  # end
end
